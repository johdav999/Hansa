# S14-P01 verification — 2026-09-07

## Result

The final MCP golden orchestration, game-side protocol operations, canonical strategic fixture adapter, semantic checkpoints, synchronized evidence writer, focused Unreal test, and explicit CI gate are implemented. Sidecar contract verification passes. The real-runtime execution is currently blocked by the repository's pre-existing unapproved windmill catalog change; this is a release blocker, not a waived test.

## Passing evidence

- `npm test` in `Tools/HansaMcp`: 28 tests passed, 0 failed. This includes full golden orchestration, MCP `test_run`, negotiated MCP-version recording, phase/checkpoint failure preservation, backward-compatible S04/S14 fixture-profile negotiation, fixed save slots, exact native capture sizes, evidence capability, and frame/transport contracts.
- Official wrapper artifact: `Saved/BuildArtifacts/20260907-074105342-hansa-mcp-contract-tests/result.json` (succeeded).
- `HansaEditor Win64 Development` compiled the final endpoint, strategic fixture, semantic surface, evidence operations, and `Hansa.Architecture.Automation.MvpGoldenEndToEnd` test. Build artifact: `Saved/BuildArtifacts/20260907-074046390-build-HansaEditor-Win64-Development`.
- `Hansa.Architecture.Automation.CapabilitiesAndPermissions`: 1 test passed, including evidence discovery and read/write permission boundaries. Artifact: `Saved/BuildArtifacts/20260907-074107184-automation-Hansa.Architecture.Automation.CapabilitiesAndPermissions`.
- The protocol-faithful fake endpoint completes the full `s14-p01-mvp-golden` path and returns a complete synchronized bundle contract.

## Blocked real-runtime evidence

Focused command:

```powershell
pwsh -NoProfile -File Scripts\RunAutomationTests.ps1 -TestFilter Hansa.Architecture.Automation.MvpGoldenEndToEnd -SkipBuild
```

Final-build artifact: `Saved/BuildArtifacts/20260907-074150865-automation-Hansa.Architecture.Automation.MvpGoldenEndToEnd`.

The canonical fixture failed before tick zero because the live catalog compiled to `97F691C37A6A4BFF`, while `FHansaLubeckScenarioInitializer::MvpRegistryHash` remains the reviewed `724BD5DE8DB9C292`. This matches the pending approval documented in `Docs/Development/WindmillPresentation.md`. S14 deliberately retains the strict content-compatibility gate.

The owned-process live MCP runner also reached the same `fixture-load-reset` checkpoint, wrote the bounded structured failure into its standard result, and stopped its exact Unreal child process. Artifact: `Saved/BuildArtifacts/20260907-073828360-mvp-golden-mcp/result.json`.

`Scripts/VerifyRepositoryConventions.ps1` is independently blocked by 26 windmill production mesh/texture assets that still reference `/Game/Hansa/Generated/Staging` and/or `Saved/GenerationJobs`. Artifact: `Saved/BuildArtifacts/20260907-072838422-repository-conventions/result.json`. These are pre-existing windmill promotion issues; S14 does not rewrite binary asset provenance.

## Closure required

After the windmill catalog is either approved with its save-compatibility decision and reviewed pin update, or returned to the reviewed catalog, rerun the focused command above and the real named-pipe `smoke:golden`. A passing run must produce `complete: true`, both native screenshot sizes, matching reset hashes, verified save/load hashes, and an authored victory bundle beneath `Saved/TestEvidence/Automation/S14P01/`. Until then, S14-P01 implementation is present but the MVP release gate is not green.
