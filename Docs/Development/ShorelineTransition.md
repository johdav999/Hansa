# Shoreline transition — 2026-09-17

The existing meadow now blends through irregular damp loam, lighter exposed silt, and a narrow dark wet edge. World-space noise varies the bands along the bank; terrain height relative to the nearest authored water level controls moisture. The same weights drive colour and roughness before the road RVT overlay, including the non-RVT fallback.

Implementation: `Source/HansaEditor/Private/World/HansaGroundSurfaceAuthoring.inl`. Reapply live using `Scripts/ApplyShoreTransitionMcp.py`; the native road material author also retains the treatment on regeneration. Existing grass and bank artwork is reused unchanged; no new generated artwork or prompt set.

Data: `SourceArt/Terrain/Lubeck/Survey_20260907/hydrology/shore-transition-data.png` is a native 2017 × 2017 RGB data raster, not a visual reference or painted asset. R stores outside-water distance / 32 metres; G stores nearest authored water level / 400 centimetres; B stores outside distance / 32 metres from the authored water-spline footprint. Its sibling JSON records source hashes, encoding and limitations. `Scripts/BuildShoreTransitionData.py` reproduces it using NumPy, Shapely, PyProj and Pillow. Existing local survey dependencies are searched first.

Imported data: `/Game/Hansa/Generated/Staging/LubeckTerrain_20260907/Textures/T_Terrain_Lubeck_ShoreBands`. Linear uncompressed data, clamped UVs, no mipmaps to avoid shifting the narrow height-dependent edge. Approximately 16 MB at runtime. Terrain material assets remain in their existing staging destinations: `M_Terrain_Hansa_Master` and `M_Lubeck_Ground`.

Water levels follow the retained native authoring contract: 90 cm for Trave and other channels, 232 cm for the two lakes, 355 cm for Wakenitz/Dükerkanal. The source geography is a modern draft, and the nearest-reach level field is approximate near confluences. Linear river strips use the retained per-point widths; geometry edits require rebuilding the data texture. The 2 m survey resolution limits bank-shape detail. This pass changes material appearance, not the measured Landscape elevation, river shape, water optics or lighting.

Validation: both terrain shaders compile through Unreal MaterialTools. The 2825 × 865 native viewport capture (`ShorelineTransition/riverbank.png`) was inspected: narrow dark contact edge, lighter irregular silt, damp soil and meadow. Native Editor Development build passed (`Saved/BuildArtifacts/20260917-194603871-build-HansaEditor-Win64-Development`). MaterialContract passed after a fresh native build (`Saved/BuildArtifacts/20260917-194727877-automation-Hansa.World.RoadTerrain.MaterialContract`). The 1920 × 1080 RoadTerrain.RealViewport regression passed, including road removal/restoration and RVT-disabled rendering (`Saved/BuildArtifacts/20260917-194801887-road-terrain-1920-1080`). Its saved gameplay view validates inland road rendering; the separate riverbank capture provides the shoreline visual inspection.
