# S14-P04 MVP acceptance report — 2026-09-07

> **Historical verdict:** this dated report evaluated the pre-EMVP-P00 acceptance contract. Its passing foundation evidence remains valid where applicable, and its blockers remain unresolved until separately closed, but it is not an acceptance report for the enhanced playable-MVP matrix now defined in [MVP.md](../../MVP.md). In particular, isolated UI captures, market-only Rostock, scripted semantic actions, and target-level Shipping scans cannot satisfy the new visual/interactive/package rows by themselves.

## Verdict

**RELEASE BLOCKED.** The S14-P04 handoff, verified command documentation, corrected multi-process authority-runner contract, fresh builds, mock authoring demonstrations, focused UI evidence, and target-level Shipping scan are complete. The integrated gameplay/save/multiplayer gates cannot initialize against the unreviewed runtime registry, reviewed deterministic goldens have drifted, production windmill packages retain transient provenance strings, and the Shipping cook fails before an expanded cooked-package audit can run. None of those failures is waived or repinned here.

The run used commit `cb2e1f2bc2bb0a25bf269d1b4a7550fd19053fc1` plus the current S00-S14 implementation worktree. The worktree contained 266 porcelain entries, so this is clean-checkout-equivalent only in the limited sense that every build/test/cook wrote a fresh timestamped artifact and no cached success was accepted. A literal clean-checkout reproduction remains required before release.

## Executed release path

| Gate | Result | Evidence |
| --- | --- | --- |
| Development Editor build | Pass | `Saved/BuildArtifacts/20260907-090117343-build-HansaEditor-Win64-Development/` |
| Repository conventions/security | Blocked | Final rerun `20260907-091936175-repository-conventions/result.json`; 12 checks pass and the only 26 failures are windmill staging/job provenance references |
| HansaMcp contracts | Pass | `20260907-090136047-hansa-mcp-contract-tests/result.json` |
| Generation worker contracts | Pass | `20260907-090136690-generation-worker-contract-tests/result.json` |
| Complete `Hansa` Unreal suite | Blocked | `20260907-090157083-automation-Hansa/`; 158 passed, 25 failed |
| OpenAI authoring mock round trip | Pass, zero live calls | `20260907-090318171-openai-authoring-acceptance/result.json` |
| Media staging/promotion/reload mock | Pass, zero live calls | `20260907-090424005-media-acceptance/result.json` |
| Native integrated gameplay golden | Blocked | `20260907-090527751-automation-Hansa.Architecture.Automation.MvpGoldenEndToEnd/`; registry rejected before tick zero |
| Save round trip fixture | Blocked | `20260907-090541971-automation-Hansa.Architecture.Automation.Save.RoundTripV1/`; same registry mismatch |
| Multiplayer authority fixtures | Blocked | `20260907-090556153-automation-Hansa.Multiplayer.Authority/`; both tests rejected the registry |
| Live MCP golden process | Blocked | `20260907-090620880-mvp-golden-mcp/result.json`; failed at `fixture-load-reset` with the registry mismatch |
| Two-player/reconnect process | Blocked after runner fix | `20260907-090721909-two-player-authority/failure.json`; server/client transports connect, runtime never becomes ready because the registry is rejected |
| Development game build | Pass | `20260907-091014668-build-Hansa-Win64-Development/` |
| Shipping cook + expanded content audit | Blocked | `20260907-091104228-media-shipping-cook/result.json`; eight Landmass references to excluded Engine editor materials/resources prevent cook completion |
| Fresh Shipping target/executable audit | Pass within stated scope | `20260907-091151563-shipping-exclusion-Win64/result.json`; receipt and 167,602,688-byte executable contain none of the forbidden tokens |
| Rendered staged harbor preview | Pass | `20260907-091243641-automation-Hansa.Architecture.StagedMedia.HarborPropPreview/` |
| Automation disabled by default | Pass | `20260907-091328312-automation-Hansa.Architecture.Automation.DisabledByDefault/` |
| Native 1280x720 and 1920x1080 UI evidence | Pass | `20260907-091342504-automation-Hansa.UI.UAT.MvpGoldenFlows.NativeScreens/` |
| Correlated shortage semantic/query evidence | Pass | `20260907-091405175-automation-Hansa.EndToEnd.ShortageDiagnosis.SemanticEvidence/` |

`Scripts/RunTwoPlayerAuthorityProof.ps1` previously serialized PascalCase keys while its JavaScript driver requires camelCase. S14-P04 fixes that contract; the rerun reached real server/client startup and exposed the independent runtime registry blocker.

## Docs/MVP.md acceptance matrix

### 10.1 Gameplay

| Acceptance item | Status | Evidence / blocker |
| --- | --- | --- |
| Scenario completes from a clean start without cheats/direct mutation | Blocked | Native and live MCP goldens reject registry `97F691C37A6A4BFF` against reviewed pin `724BD5DE8DB9C292` before tick zero. |
| Same seed and command stream produce the same checksum | Blocked | The 80,000-tick S14-P03 soak is repeatable, but reviewed `foundation_determinism_v1` expects `C827BB245C79D327` while the current six-tick result is `F50841E8464CF363`; other fixture goldens also drift. |
| Goods and money conservation invariants pass | Pass | S14-P03 `Hansa.Simulation.Inventory.PropertyInvariants` and non-runtime simulation coverage pass. |
| Local prices respond in the expected direction and remain bounded | Pass | S14-P03 market bounds, cadence, causal-query, alert, and shortage/recovery tests pass. |
| AI uses normal commands and resources | Blocked | Command architecture exists, but both live AI tests fail initialization on the reviewed registry mismatch. |
| Save/load preserves authoritative checksum | Blocked | Envelope/corruption cases pass, but the release fixture and runtime continuation cannot initialize against the reviewed registry. |
| Two clients do not diverge or access unauthorized private data | Blocked | Unit/process contracts cover ownership and scoped projections, but both native fixtures and the corrected multi-process proof are blocked by runtime initialization. |

### 10.2 Editor

| Acceptance item | Status | Evidence / blocker |
| --- | --- | --- |
| All MVP definitions authored without C++ values/raw serialization | Blocked | Most editor coverage passes, but `Hansa.Architecture.Authoring.GoldenSchema` and the authored economic registry golden are stale in the full suite; complete reviewed coverage is not green. |
| Reflected field gets generic schema/edit support and missing metadata fails CI | Blocked | Metadata enforcement tests exist, but the reviewed schema-golden mismatch prevents a release-pass claim. |
| OpenAI cannot bypass schema/revision/reference/range/domain validation | Pass | Mock authoring acceptance, stale-apply rejection, Undo/Redo, and nine native generation-worker architecture tests pass. |
| Generated media cannot bypass staging/provenance/QA/approval | Blocked | Mock media promotion and fresh-editor reload pass, but 26 production windmill assets retain `Saved/GenerationJobs` and/or `/Game/Hansa/Generated/Staging` strings. |
| Normal CI uses mocks and spends no credits | Pass | Worker contracts and both mock acceptance results record no live calls; media result records zero money spent. |

### 10.3 UI and automation

| Acceptance item | Status | Evidence / blocker |
| --- | --- | --- |
| Every golden-path action has a stable semantic target | Blocked | Native semantic/UI tests pass, but the complete real-runtime golden path cannot start; fake-endpoint success is not a substitute. |
| Displayed authoritative values agree with correlated queries | Pass | Focused rendered shortage semantic/query evidence passes at both required resolutions. |
| Tests wait on state/tick conditions, not arbitrary sleeps | Pass | MCP contracts and bounded native/process wait contracts pass; timeout failures retain their checkpoint. |
| Native 1280x720 and 1920x1080 captures have no critical clipping/overlap | Pass | The focused `-WithRendering` native UI gate passes and retains 14 native captures; see S14-P02 for inspection details. |
| Failed end-to-end test has sufficient reproduction evidence | Pass | Live MCP records `fixture-load-reset`, exact hashes, logs and result JSON; multiplayer records config, process logs, timeout and failure JSON. |
| Automation-disabled Development play opens no endpoint/incurs no per-frame work | Pass | Focused `Hansa.Architecture.Automation.DisabledByDefault` passes; Shipping also omits the automation module/token markers. |

### 10.4 Release boundary

| Acceptance item | Status | Evidence / blocker |
| --- | --- | --- |
| Shipping contains no editor/automation/MCP/worker/provider/staging/QA fixture | Blocked | Fresh receipt/executable scan passes, but the cook fails and no expanded packaged tree exists to inspect; production provenance audit also fails. |
| Shipping ignores development flags and opens no endpoint | Blocked | Target/module exclusion and Development disabled-default tests pass, but a packaged Shipping process was not produced and launched. |
| Packaged vertical slice launches and completes without development tools | Blocked | No successful package exists; cook fails on Landmass editor-resource references. |

## MVP definition-of-done mapping

| Docs/MVP.md §14 item | Status |
| --- | --- |
| Integrated authoring, play and automated demo from a clean checkout | Blocked: mock authoring passes; play/automation and literal clean checkout do not. |
| Golden scenario winnable manually and through controlled automation | Blocked: no passing manual evidence; automation rejects the registry. |
| Deterministic, save/load and two-client authority checks | Blocked: soak passes, reviewed goldens/save/multiplayer do not. |
| Game/editor/automation parity | Blocked: runtime registry, schema golden and full golden remain red. |
| Native screenshot and semantic evidence | Pass for the focused UI/shortage flows. |
| Generated data/media traceable, validated, explicitly approved | Blocked for production windmill packages; mock acceptance alone is insufficient. |
| Normal CI performs no billable provider calls | Pass. |
| Playable Shipping build excludes development systems | Blocked: target executable exclusion passes, cook/package/playability does not. |

## Required closure before release

1. Restore the runtime content set to reviewed hash `724BD5DE8DB9C292`, or review/approve the current catalog and deliberately update every compatibility/golden consequence together.
2. Review rather than silently repin `foundation_determinism_v1` and all stale shortage/production/schema goldens.
3. Reimport/resave or otherwise clean the 26 windmill production packages so no transient staging/job reference remains, then rerun repository conventions.
4. Resolve the Landmass startup-package references to Engine editor-only resources and obtain a successful Shipping cook plus expanded cooked-tree scan.
5. Rerun `Scripts/InvokeCI.ps1 -TestFilter Hansa` from a literal clean checkout, complete the manual authoring/play path, and launch/complete the packaged vertical slice without development tools.

## EMVP-P01 closure note (2026-09-07)

Required-closure item 1 is resolved by EMVP-P01. The approved mill presentation assignment is catalog version 2, registry `97F691C37A6A4BFF`; catalog version 1 remains recorded as `724BD5DE8DB9C292`. The implementation pins a per-definition golden manifest and deliberately retains exact-hash save rejection, so version-1 saves require a new game rather than a silent migration. This note does not change the historical S14 verdict or resolve items 2–5.
