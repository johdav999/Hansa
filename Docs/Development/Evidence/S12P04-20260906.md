# S12-P04 acceptance implementation — 2026-09-06

## Delivered

- `Scripts/RunOpenAIAuthoringAcceptance.ps1`: one mock-only gate that builds when needed, runs worker contracts, exports a native definition contract, runs the persistent mock worker, feeds verified output back to native acceptance, runs bridge tests, and combines evidence.
- `Tools/HansaGenerationWorker/scripts/authoring-acceptance.js`: uses only the deterministic mock, recovers a queued native-contract job after reopening its store, completes exactly one job, verifies the staged output and preserves manifests/provenance.
- Native recipe acceptance now accepts worker-produced JSON in the controlled test runner, checks stale Apply preserves a concurrent edit, verifies previews leave accepted content untouched, and emits typed fixture metrics plus registry/state hashes.
- CI invokes the dedicated gate independently of its general test filter. The manual zero-credit demo and secure setup/recovery coverage map are updated.

## Verification

The complete runner succeeded in `Saved/S12P02Verification/Saved/BuildArtifacts/20260906-195010959-openai-authoring-acceptance/`:

- 20 mock/recorded worker tests passed.
- Native acceptance passed once to export the contract and once using the actual persistent-worker proposal.
- Nine GenerationWorker architecture tests passed.
- The final combined report confirms `proposalSource=persistent-mock-worker`, zero live provider calls, stale Apply rejection and Undo/Redo verification.
- The final changed C++ test, runner and Node driver are byte-identical to the isolated tested copies.
- `InvokeCI.ps1` syntax validation passed. The entire broad CI pipeline was not rerun; the new gate was executed directly.
- Fresh Shipping receipt/executable exclusion passed: `Saved/BuildArtifacts/20260906-195158784-shipping-exclusion-Win64/result.json`.

## Limits

The main Editor DLL remains locked by the open user session. The existing isolated build was used; its `Content/Mesh` junction reads the fixture mesh dependencies, while acceptance modifies transient definitions only. Close the main editor and rebuild to load the new test code in that checkout.

The round-trip driver verifies the persistent service and artifact boundary; it does not claim to drive Slate controls or independently retest pipe framing. Dedicated worker/bridge tests cover the transport. The manual UI walkthrough is documented, not manually performed. No raster design or image generation was needed, no native screenshot QA was run, and no live API request was made. Shipping receipt/executable checks do not replace the packaged/cooked/depot release audit.

## Tested source hashes

- `Source/HansaEditor/Private/Tests/HansaOpenAIAcceptanceTests.cpp`: `9fa541aa24165c2490209343d5d50a7c2b1f750dd2c7cf34e9c6851f3b714cc0`
- `Scripts/RunOpenAIAuthoringAcceptance.ps1`: `dd848c47b8c7bd74c02b5eb3edcc6dfb5794ac281fe9ab2f6639ef41ea9b7cc1`
- `Tools/HansaGenerationWorker/scripts/authoring-acceptance.js`: `a4ac65800d9239257f3921113e92583bcf9eea1b9ed7023ed2c00046adf12f34`
