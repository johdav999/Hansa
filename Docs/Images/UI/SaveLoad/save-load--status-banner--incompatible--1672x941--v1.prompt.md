# Incompatible-save status reference prompt

- Intended use: actionable incompatibility banner visual reference
- Native dimensions: 1672 × 941 px
- Aspect ratio: 16:9
- Generation mode: built-in ImageGen, new image using the save/load references as style anchors
- Shipping status: visual reference only; implemented with native Slate
- Revision: v1

## Final prompt

ui-mockup. Match the Hansa Save and Load visual family exactly: deep navy shell, warm linen status surface, restrained aged brass border, ink-dark text, subtle merchant-ledger grain. Create one isolated wide actionable error banner on a plain dark field. It communicates “Incompatible save”, identifies “Content registry differs” as the cause, and reserves a second line for the remedy “Restore the matching content build, then try this slot again.” Use an error octagon or broken-seal glyph plus text so status is not color-only, with a muted red accent that preserves contrast. Leave wide safe margins and localization expansion. No controls, no contact sheet, no baked production text, no glass, no decorative clutter.

## Inspection

Accepted at original 1672 × 941 resolution. Error, cause, and remedy form a clear reading order; the icon provides a non-color cue and all edges/shadows remain inside the canvas.