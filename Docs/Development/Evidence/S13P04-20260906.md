# S13-P04 implementation evidence — 2026-09-06

Implemented [connected media acceptance and recovery](../MediaAcceptance.md), including a repeatable manual mock demo and separately authorized optional live smoke instructions. No live provider calls or real credit spending occurred.

## Delivered

- `Tools/HansaGenerationWorker/scripts/media-acceptance.js`: real Tripo/ElevenLabs adapters with injected mock HTTP, real MP3 decoding, durable progress assertions, queued SFX cancellation and explicit revision-two retry, a killed worker process at persisted prop Running state, fresh-process resume without prop resubmission, take-two audio retention and immutable source hashes.
- `Source/HansaEditor/Private/Tests/HansaMediaAcceptanceTests.cpp`: native import of those exact worker-retained sources, existing prop-scene render, unapproved-promotion rejection, injected pre-commit failure/rollback/retry, promotion of all three roles and fresh-Editor package/identity/subtitle/QA/provenance verification after transient worker-state deletion.
- `Scripts/RunMediaAcceptance.ps1`: aggregate evidence, two Editor invocations, transient-state archive/deletion, retained source/approval evidence and bounded fixture cleanup. Included in `Scripts/InvokeCI.ps1`.
- Worker regression test proves ambient live enable flags cannot activate real transports, duplicate demo directories fail, changed cleanup paths are rejected, source hashes survive worker-state deletion, and Unreal digits-only staging GUID folders are removed.
- Existing gameplay models, schema/profile versions, runtime dependencies and production approval APIs are unchanged. Automatic approval/playback surrogates are explicit test-only behavior, restricted to mathematical mock manifests.

## Verification

| Check | Result | Artifact under Saved/BuildArtifacts |
| --- | --- | --- |
| Connected media acceptance | Passed for prop, SFX and speech through both Editor invocations | `20260906-221654180-media-acceptance/result.json` |
| Worker contracts | 58/58 passed | `20260906-221717999-generation-worker-contract-tests/NodeTests.log` |
| Editor build | New native test compiled and linked in Development module 1315 | `S13P04-editor-build.log` |
| Shipping exclusion | Passed, receipt/executable scope | `20260906-221513698-shipping-exclusion-Win64/result.json` |
| Production references | Passed, dependency-only scope | `20260906-221555832-media-shipping-audit/result.json` |
| Repository conventions | Failed on existing Windmill serialized transient/staging source paths | `20260906-221512179-repository-conventions/result.json` |

Unreal's cached source list was refreshed with `-gather` to include the new test. A suffixed module avoids replacing DLLs held by running editors; no user Editor was terminated. The native acceptance filter is `Hansa.Integration.Authoring.MediaAcceptanceFlow`; the wrapper requires both native evidence documents and three successful roles, so an unconfigured standalone skip cannot satisfy the acceptance runner.

The final worker log records three Queued jobs, SFX Cancelled/retried, prop Running/restart and three recovered Review results. SFX retained revision is two; prop and speech are revision one. Both audio roles retain output index one, the second take. The restarted transport asserts exactly four audio POSTs and no new prop POST. Each role's event stream contains the expected lifecycle and recovery event.

The first Editor run rejects unapproved promotion and proves rollback before committing the prop, then promotes all three roles with explicit test-only approval. The second Editor run occurs after actual deletion of this run's Saved job store, loads final native packages, checks stable metadata and reruns the mesh/audio contracts and retained promotion verification. The source/manifest/approval chain is copied to the evidence bundle before disposable fixture cleanup. Source, staging, final fixture and job directories were independently checked absent after the final run.

## Preview and fixture provenance

Components reused: native mesh/Wave asset formats and the existing neutral floor, axis guide, camera, lighting and capture scene. No new UI screen, raster asset, ImageGen mode or prompt set was introduced.

The connected run produces `harbor-preview.png` at native 1280×720 with no resampling. The native capture was inspected at original resolution: the mathematical cube has a readable silhouette and visible faces, safe margins and clear ground contact; renderer dithering remains. This is a test/reference capture, not production artwork. The mesh source SHA-256 is `f24be5bd2465af063feb1521a38f5e635a7ad961724bff3c3041c5f75c7ab8a5`. Audio fixture origin and source hashes are recorded in `Tests/Golden/Media/audio-take.source.md`; the source bytes are the existing original mathematical fixtures.

## Limits and remaining gates

Automated audio is a decoded sine-tone fixture with subtitle metadata, not generated speech. Native CI uses test-only playback and reviewer surrogates; it does not prove human listening, intelligibility, rights to a real voice or artistic quality. The clean manual demo is documented and runnable, but no completed human review is claimed. A live run requires a separately approved budget and rights review.

Shipping executable exclusion and production dependency audits pass, but they do not prove cooked-package exclusion. The existing Windmill serialized source-path violations remain untouched, and the previously documented Landmass references to EngineEditorResources still block the full cook. No new full cook was attempted. See [S13-P01 evidence](S13P01-20260906.md) for that blocker. The full integrated MVP/Shipping manual demo is not declared complete by this task.
